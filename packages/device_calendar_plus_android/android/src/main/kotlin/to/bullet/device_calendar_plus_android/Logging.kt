package to.bullet.device_calendar_plus_android

/**
 * Logcat tag for the best-effort steps that run after a write has
 * committed, whose failures no caller hears of: a thisAndFollowing update
 * split's [DetachedOccurrenceCarry], and its sweep when the split clears
 * the rule.
 */
internal const val LOG_TAG = "DeviceCalendarPlus"
